#!/usr/bin/perl

use warnings;
use strict;

die "program lignes nombre retourligne" unless (scalar(@ARGV) == 3);

my ($l, $n, $o) = @ARGV;

for (my $i = 0; $i < $l; $i++) 
{
	for (my $j = 0; $j < $n; $j++) 
	{
		print(chr(int(rand(92)) + 33))
	}
	if ($o == 1) {
		print "\n";
	}
}
